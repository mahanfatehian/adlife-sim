"""Packaged demo project: the files `adlife init` copies into a new study.

Loaded only through :mod:`importlib.resources`, never through paths relative to
``__file__``, so the resources survive installation as a wheel or a frozen binary.
"""

from __future__ import annotations
